# LibreChat MCP 配置文件 - stdio 方式（备选）
# 说明：使用 stdio 方式，需要在配置中指定数据库环境变量

{
  "mcpServers": {
    "oilfield-wells": {
      "command": "python",
      "args": ["d:\\work\\oilMCP\\oilfield_wells_mcp.py"],
      "env": {
        "DB_HOST": "localhost",
        "DB_PORT": "5432",
        "DB_NAME": "rag",
        "DB_USER": "postgres",
        "DB_PASSWORD": "postgres",
        "DEV_MODE": "true"
      }
    },
    "oilfield-dailyreports": {
      "command": "python",
      "args": ["d:\\work\\oilMCP\\oilfield_dailyreports_mcp.py"],
      "env": {
        "DB_HOST": "localhost",
        "DB_PORT": "5432",
        "DB_NAME": "rag",
        "DB_USER": "postgres",
        "DB_PASSWORD": "postgres",
        "DEV_MODE": "true"
      }
    }
  }
}
